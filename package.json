{
  "name": "actwyn",
  "version": "0.0.0",
  "private": true,
  "description": "Personal Agent P0 — Telegram-first personal agent on Claude (Bun).",
  "type": "module",
  "engines": {
    "bun": "1.3.11"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "bunx --bun tsc --noEmit",
    "lint:redactor": "bun run scripts/check-single-redactor.ts",
    "ci": "bun run lint:redactor && bun run typecheck && bun test"
  },
  "devDependencies": {
    "typescript": "5.6.3",
    "@types/bun": "1.3.1"
  }
}

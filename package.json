{
  "name": "actwyn",
  "version": "0.0.0",
  "private": true,
  "description": "Personal Agent P0 — Telegram-first personal agent on Claude (Bun).",
  "type": "module",
  "engines": {
    "bun": "1.3.11"
  },
  "scripts": {
    "dev": "doppler run -- bun run src/main.ts",
    "test": "bun test",
    "typecheck": "bunx --bun tsc --noEmit",
    "lint:redactor": "bun run scripts/check-single-redactor.ts",
    "lint:thin-docs": "bun run scripts/check-thin-docs.ts",
    "ci": "bun run lint:redactor && bun run lint:thin-docs && bun run typecheck && bun test",
    "docs:generate:schema": "bun run scripts/generate-schema-doc.ts",
    "bench:context": "bun run scripts/bench-context-compiler.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.12",
    "typescript": "5.6.3"
  }
}

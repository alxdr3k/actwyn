node_modules/
dist/
.env
.env.*
!.env.example
*.db
*.db-wal
*.db-shm

# Runtime state directories — never source code. Anchored so
# `src/memory/` and `test/memory/` are not accidentally ignored.
/data/
/logs/
/memory/
/artifacts/
/transcripts/
.DS_Store
